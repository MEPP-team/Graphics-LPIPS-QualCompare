# Compute correlation summaries (PLCC/SROCC) from the evaluation outputs.
# Replace the <...> placeholders (use the SAME values as the evaluation step):
#   <N>              number of views (e.g. 8)
#   <View_Method>    e.g. Y_fixed_0.3
#   <Render_Method>  e.g. New_Render
# Keep --use_folds only if the evaluation was run with folds.
# Single line: copy the whole command as-is (works in cmd, PowerShell and bash).

python correlation_VP.py -m TMQ_<N>VP_example --use_folds -v <N> -vm <View_Method> -rm <Render_Method> -db TMQ --out_root ./out
