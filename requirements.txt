# Software environment reported in the paper (§3.8):
#   Python 3.12.10, PyTorch 2.7.0 (CUDA 12.8, cuDNN 9.7.1), OpenCV 4.11.0.86.
#
# Install the CUDA build of PyTorch FIRST (PyPI ships the CPU build), e.g.:
#   pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128
# then:
#   pip install -r requirements.txt
#
# Versions pinned below match the paper; the remaining packages were not
# version-locked in the paper (kept as lower bounds).
torch==2.7.0
torchvision==0.22.0
opencv-python==4.11.0.86
numpy>=1.14.3
scipy>=1.0.1
scikit-image>=0.19.0
matplotlib>=1.5.1
tqdm>=4.28.1
jupyter
statsmodels
rapidfuzz
dominate
ipython
pandas
torchmetrics
